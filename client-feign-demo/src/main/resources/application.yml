eureka:
    client:
        fetch-registry: true
        register-with-eureka: true
        service-url:
          defaultZone: http://localhost:8761/eureka/ #注册中心的ip
spring:
  application:
    name: client-test-feign
server:
  port: 8002